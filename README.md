# Purpose

This repository contains the the the Jenkins files that I wrote and configurations files to setup VirtualBoxthe the Jenkins files that I wrote and configurations files to setup VirtualBox environment for checking purposesthe the Jenkins files that I wrote and configurations files to setup VirtualBox environment for checking purposesenkins files that I wrote and configurations files to setup VirtualBox environment for checking purposes

# Content

## jenkins directory

Contains Jenkinsfiles that I wrote.

## vagrant directory

Contains Vagrantfile and Ansible playbook to setup Jenkins server locally.

## terraform directory

Contains Terraform configuration file to setup Jenkins server in AWS cloud. The CICD_Setup subdirectory contains Terraform configuration files to setup ECS service and ECR service.
